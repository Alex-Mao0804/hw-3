import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#518581",
    },
    secondary: {
      main: "#afadb5",
    },
    error: {
      main: "#d32f2f",
    },
  },
});

export default theme;
